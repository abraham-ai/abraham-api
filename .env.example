# =============================================================================
# PRIVY AUTHENTICATION
# =============================================================================
# Get these from https://dashboard.privy.io
PRIVY_APP_ID=your_privy_app_id_here
PRIVY_APP_SECRET=your_privy_app_secret_here

# =============================================================================
# THESEEDS CONTRACT (L2 - Base)
# =============================================================================
# TheSeeds contract address on Base L2
# After deploying, update this with your contract address
L2_SEEDS_CONTRACT=0x878baad70577cf114a3c60fd01b5a036fd0c4bc8

# Network: "baseSepolia" for testnet, "base" for mainnet
NETWORK=baseSepolia

# =============================================================================
# BACKEND WALLET (For Gasless Operations)
# =============================================================================
# Private key for backend wallet (MUST have CREATOR_ROLE + RELAYER_ROLE)
# ⚠️ IMPORTANT: Use a DIFFERENT wallet from your deployer wallet
# ⚠️ This wallet will pay gas for backend-signed transactions
# Format: With or without 0x prefix (both work)
RELAYER_PRIVATE_KEY=0x...

# =============================================================================
# ADMIN AUTHENTICATION
# =============================================================================
# Secret key for admin-only endpoints:
# - POST /api/admin/update-snapshot (update NFT snapshot + merkle tree)
# - POST /api/admin/reload-snapshot (refresh snapshot cache)
# - POST /api/seeds (backend-signed seed creation)
#
# ⚠️ IMPORTANT: This key provides full admin access - keep it secure!
# ⚠️ Admin endpoints only require this key, NOT Privy authentication
#
# Generate a strong random key (choose one method):
#   - OpenSSL:  openssl rand -hex 32
#   - Node.js:  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#   - Online:   Use a password generator (32+ characters)
#
# Example: ADMIN_KEY=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456
ADMIN_KEY=your_secret_admin_key_here

# =============================================================================
# VERCEL CRON JOB AUTHENTICATION
# =============================================================================
# Secret for authenticating Vercel cron job requests
# Used by the daily snapshot update cron job
#
# ⚠️ IMPORTANT: Set this in Vercel dashboard as an environment variable
# Vercel will automatically send this as Authorization: Bearer <token>
#
# Generate a strong random key (minimum 16 characters):
#   - OpenSSL:  openssl rand -hex 32
#   - Node.js:  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# How to set in Vercel:
# 1. Go to your project settings in Vercel dashboard
# 2. Navigate to Environment Variables
# 3. Add CRON_SECRET with a random string (32+ characters recommended)
# 4. Redeploy your project
#
# Example: CRON_SECRET=cron_a1b2c3d4e5f6789012345678901234567890abcdef1234567890
CRON_SECRET=your_cron_secret_here


# =============================================================================
# RPC URLS
# =============================================================================
# Base Sepolia (Testnet) - Public RPC or get from Alchemy/Infura
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
L2_RPC_URL=https://sepolia.base.org

# Base Mainnet (Production) - Get from Alchemy/Infura for better reliability
BASE_MAINNET_RPC_URL=https://mainnet.base.org

# Ethereum Mainnet - For FirstWorks NFT snapshot generation
# Get a free RPC URL from:
# - Alchemy: https://www.alchemy.com/
# - Infura: https://www.infura.io/
FIRSTWORKS_RPC_URL=your_ethereum_rpc_url_here
# Example: FIRSTWORKS_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# FirstWorks NFT Contract Address (L1 Ethereum)
FIRSTWORKS_CONTRACT_ADDRESS=0x8F814c7C75C5E9e0EDe0336F535604B1915C1985

# =============================================================================
# DEPLOYMENT & ADMIN OPERATIONS
# =============================================================================
# Private key of wallet that deployed the contract (has ADMIN_ROLE)
# This wallet can:
# - Update merkle root (for snapshot updates)
# - Grant/revoke CREATOR_ROLE and RELAYER_ROLE
# - Pause/unpause contract
# ⚠️ Keep this extremely secure - it controls all contract permissions
# ⚠️ Only needed for admin operations, not regular API usage
# Format: With or without 0x prefix (both work)
DEPLOYER_PRIVATE_KEY=0x...

# =============================================================================
# CONTRACT VERIFICATION
# =============================================================================
# Basescan API key for contract verification
# Get your API key from:
# - Mainnet: https://basescan.org/myapikey
# - Testnet: https://sepolia.basescan.org/myapikey
# Note: The same API key works for both mainnet and testnet
BASESCAN_API_KEY=your_basescan_api_key_here

# =============================================================================
# VERCEL BLOB STORAGE (Optional but Recommended)
# =============================================================================
# Vercel Blob storage token for automatic snapshot/merkle tree uploads
# This eliminates the need for manual git commits after snapshot updates
#
# How to get your token:
# 1. Go to https://vercel.com/dashboard
# 2. Select your project
# 3. Go to "Storage" tab
# 4. Create a Blob Store (if you haven't already)
# 5. Copy the "Read-Write Token"
#
# Benefits:
# - No manual git commits needed
# - Automatic cleanup of old snapshots (keeps last 5 versions)
# - Fast CDN-backed reads
# - 5GB free tier
#
# If not configured, snapshots will only be saved locally
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# =============================================================================
# OPTIONAL: Additional Configuration
# =============================================================================
# Port for development server (default: 3000)
# PORT=3000

# Log level (default: info)
# LOG_LEVEL=info
